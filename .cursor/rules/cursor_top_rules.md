# Cursor 최상위 규칙

여기에 규칙을 작성하세요. 웹에서 저장하면 서버의 이 파일에 반영됩니다.

## 대시보드 동기화 고정 규칙

- 밴다이어그램(명령 프로세스) 또는 중요 제한점(기기별/클라우드별 제한)을 수정할 때는 아래 2개를 반드시 함께 수정한다.
  - `web-browser/static/index.html`
  - `web-browser/static/app.js`
- 스타일 변경이 필요하면 `web-browser/static/styles.css`도 함께 수정한다.
- 위 내용 수정 후에는 웹 브라우저 대시보드가 즉시 반영되도록, 같은 작업 턴에서 서버/정적 파일 변경을 완료 상태로 커밋한다.

## 자동화 우선 운영 규칙

- 모든 코드/설정/배포/동기화 작업은 기본적으로 **최대의 자동화**를 목표로 처리한다.
- 보안/권한/파괴적 변경(데이터 삭제, 비가역 작업)처럼 중요한 경우를 제외하고는 사용자에게 중간 확인을 요구하지 않고 직접 진행한다.
- 사용자에게 질문하거나 확인이 필요한 문장을 보낼 때는 반드시 맨 앞에 `[check]` 접두어를 붙인다.
- 모든 명령을 전부 완료한 최종 보고는 맨 앞에 `[done]`을 붙인다.
- 진행 중 문제가 생기거나 작업이 잘못된 경우는 맨 앞에 `[error]`를 붙인다.
- 처음부터 실행 불가이거나 사용자 질문 자체를 이해하지 못한 경우는 맨 앞에 `[no]`를 붙인다.
- 기술적으로 명시적/구조적 한계가 있을 때는 맨 앞에 `[limit]`를 붙인다.
- Cursor 또는 Google Cloud의 원래 목적상 되어야 하는 동작이 실패하는 버그는 맨 앞에 `[bug]`를 붙인다.
- 위 분류 어디에도 포함되지 않으면 맨 앞에 `[?+문제점의 영어단어]` 형식을 사용한다.
