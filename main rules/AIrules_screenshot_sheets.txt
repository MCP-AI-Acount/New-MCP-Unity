# 스크린샷→시트 파이프라인 (결론)

- 인증: Sheets 는 OAuth `token_sheets.json` + `credentials.json` (NewMCP/MCP_Server).
- OCR: 로컬 Tesseract; 한글은 tesseract-lang 필요. 클라우드 Vision API는 이번 구현에 포함하지 않음.
- 양식: JSON 템플릿의 regex 는 사용자 스크린샷에 맞게 수정하는 것이 전제.
